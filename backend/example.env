# MongoDB Atlas Configuration (REPLACE WITH YOUR ACTUAL CONNECTION STRING)
# Follow MONGODB_SETUP.md to get your real connection string
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/gorbagana-trash-tac-toe?retryWrites=true&w=majority

# Server Configuration  
PORT=3001
NODE_ENV=development

# CORS Origins
CORS_ORIGINS=http://localhost:3000,https://your-app.netlify.app

# NOTE: Without MONGODB_URI, the app will use file storage (current behavior)
# This will fix the "undefined" stats errors 