# Gorbagana Trash Tac Toe Backend Configuration

# Database Configuration
# For development: Use file-based storage (leave empty)
# For production: Use MongoDB Atlas connection string
MONGODB_URI=

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,https://your-netlify-app.netlify.app

# Example MongoDB Atlas URI (replace with your actual connection string):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/gorbagana-trash-tac-toe?retryWrites=true&w=majority 